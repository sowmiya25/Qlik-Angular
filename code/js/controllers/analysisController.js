app.obj.angularApp
	.controller('controller.analysisController', function ($scope, $rootScope, $location, $injector, api, utility) {
		 $scope.showobjectzoomed = function(){
		
		   $scope.collapsedchart = true;
				
		}
	});
